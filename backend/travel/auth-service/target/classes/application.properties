# 1. Server Configuration
server.port=8081
spring.application.name=auth-service

# 2. Database Configuration (MySQL)
# Update 'travel_auth_db' to whatever you named your database in MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/travel_auth_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
# REPLACE 'root' WITH YOUR ACTUAL MYSQL PASSWORD
spring.datasource.password=root123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 3. JPA / Hibernate Configuration
# 'update' automatically creates/updates tables based on your Entity classes
spring.jpa.hibernate.ddl-auto=update
# Shows the SQL queries in the console (useful for debugging)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 4. Logging
logging.level.org.springframework.security=DEBUG

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=experimentone1234@gmail.com
spring.mail.password=rdhy kkdq saae akte
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true